import 'package:flutter/material.dart';
import 'package:first_project/home/home_page.dart';

class MyApp extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      title: "Primeira Aplicação",
      home: HomePage(),
    );
  }
}
